# SETUP
## In case you have Docker, run the following Command:
docker run --name postgres -e POSTGRES_PASSWORD=docker -p 5432:5432 -d postgres

# All instructions necessary to configure the .env file containing the ORM configs are in the .env-example file.
# On each folder, type in the terminal "yarn" or "npm install" and the required dependencies will be installed in your machine
